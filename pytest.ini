[pytest]
testpaths = tests
markers =
    fast: tests that need no Ray cluster and no GPU
    integration: tests that require a running Ray cluster
    gpu: marks tests as GPU tests (deselect with '-m "not gpu"')
    asyncio: async tests
